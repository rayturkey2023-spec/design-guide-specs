# 按钮（Button）组件规范

## 使用场景

- 触发操作、提交表单、页面跳转等交互行为
- 区分主次操作优先级，引导用户完成核心流程
- 活动类场景使用活动主题色按钮提升转化

## 禁止场景

- 禁止同屏出现 2 个 Primary 按钮
- 禁止 Danger 按钮无二次确认直接触发破坏性操作
- 禁止按钮文字超过 2 行（文字过长时截断或缩短文案）
- 禁止弹窗/基础场景中按钮宽度完全自由伸缩（须使用固定宽度）

---

## 一、主按钮（Main Button）

### 变体与使用场景

| 变体 | 使用场景 | 每屏限制 |
|------|---------|---------|
| Primary | 唯一主操作（提交、确认、下一步）| 每屏最多 1 个 |
| Secondary | 次要操作（取消、返回、重置）| 不限 |
| Ghost / Outline（线框）| 低优先级操作、多选项并列 | 不限 |
| Danger | 删除、注销等不可逆操作 | 每屏最多 1 个，且必须有二次确认 |
| Text | 页面内联操作、链接跳转 | 不限 |

### 尺寸规范

| 尺寸      | 高度      | 字号      | 使用场景         |
| ------- | ------- | ------- | ------------ |
| 44、40pt | 44、40pt | 16、14pt | 页面主操作、常规主要操作 |
| 30pt    | 30pt    | 12pt    | 常规表单、卡片内操作   |
| 28pt    | 28pt    | 12pt    | 次要操作、紧凑布局    |
| 24pt    | 24pt    | 10pt    | 卡片内操作、极小尺寸   |
| 20pt    | 20pt    | 10pt    | 最小规格，谨慎使用    |

> 图标尺寸：大号按钮（44-30pt）配 20×20pt 图标；小号按钮（28/24/20pt）配 12×12pt 图标

### 布局约束

- 移动端底部主操作按钮：宽度撑满（减去左右边距），高度使用 40pt
- 并列按钮（如取消+确认）：等宽排列，间距固定
- 空间不足时，横排改为竖排，主按钮在上，次按钮在下
- 弹窗内按钮须使用固定宽度，不随文案自由拉伸
- 默认只展示按钮的主标题
- 可展示主标题+辅助引导决策信息，但辅助引导信息仅展示1个，超出上下轮播展示
- 若需进一步强化主操作，可以在按钮主标题前加单色图标

### 颜色规范

#### 基础色

| 用途          | 色值        | 说明          |
| ----------- | --------- | ----------- |
| 深色按钮背景      | `#000000` | 深色背景按钮      |
| 白色按钮背景      | `#FFFFFF` | 白底按钮        |
| Disabled 背景 | `#BBBBBB` | 禁用态         |
| 浅灰分割/弱化     | `#E5E5E5` | 浅灰状态        |

#### 活动主题色（按业务场景控制）

| 色值 | 适用场景 |
|------|---------|
| `#FF3351` | 新人活动、海外活动类场景 |
| `#FF4242` | 礼品卡、红包相关页面 |

> 活动色允许根据业务需要替换整套界面主题色，不属于默认主色

#### 背景适配样式

| 样式类型 | 按钮配色方案 |
|---------|------------|
| 深色背景样式 | 白字/浅色字，高对比前景 |
| 线框背景样式（Outline）| 白底深字，描边边框 |
| 白色背景样式 | 黑字或品牌色背景 |
| 反白按钮样式 | 深灰底高对比，强调轮廓对比 |

### 状态

- Default / Active（点击态）/ Disabled / Loading

### 交互规范

#### 点击态

- 点击态统一处理：**按钮整体不透明度降低至 60%**
- 适用于所有背景样式下的按钮，提供一致的按压反馈
- 点击前后需有明显可感知的状态变化

---

## 二、局部操控按钮（Section Control Buttons）

### 简述

用于对页面某一局部模块/区域进行操作控制，常用于局部场景下的按钮操作，以局部而非全局的操作为主。

### 尺寸规范

| 规格 | 高度 | 字号 | 图标尺寸 |
|------|------|------|---------|
| 大号 | 36pt | 14pt | 20×20pt |
| 中号 | 30pt | 12pt | 20×20pt |
| 中小号 | 28pt | 12pt | 12×12pt |
| 小号 | 24pt | 10pt | 12×12pt |
| 极小号 | 20pt | 10pt | 12×12pt |

### 文案截断规则

- 按钮内文本过长时，优先显示文案前部，末尾截断显示省略号（`…`）
- 不破坏按钮整体高度和对齐

### 可行性（Accessibility）布局场景

| 布局方式 | 使用场景 |
|---------|---------|
| 单按钮 | 单个主操作，居中或靠近主内容区 |
| 双按钮横排 | 空间充足时，主按钮在右，次按钮在左，视觉等重 |
| 双按钮竖排 | 空间不足时，主按钮在上，次按钮在下 |
| 弹窗内按钮 | 固定宽度，不自由拉伸，主次按钮有明显视觉区分 |
### 布局约束

- 允许使用导角，导角度数根据实际需求自定义，但必须为双数
- 特殊样式包含文案+图标样式、纯文字样式、纯图标样式；用在多个子模块的局部操作，或者信息层级比较复杂的页面的局部操作，需要通过差异化样式来起到层级差异的作用
	- 间距：间距为最小间距，具体可根据实际场景自定义，但必须为双数


### 颜色规范同主按钮
### 交互规范同主按钮


## 三、悬浮按钮（Floating Button / FAB）

### 简述

悬浮按钮（FAB）是一个独立的悬浮按钮，可让用户在应用中快速执行操作。通常位于屏幕的右下角，不支持手动关闭。

### 结构规范

```
页面（全高）
└── 悬浮按钮（右下角固定定位）
    ├── 圆形按钮：44×44px（仅图标）
    └── 竖版按钮：44×54px（图标 + 文案）
        ├── 图标区（圆形，44×44px）
        └── 文案区（图标下方，最多 60 字符，超出省略）
```

### 尺寸规范

| 形态 | 尺寸 | 说明 |
|------|------|------|
| 圆形（常规尺寸）| 44×44px | 纯图标，无文案 |
| 竖版（按钮+文案）| 44×54px | 图标 + 下方文案 |

### 位置规范

- **默认位置**：页面右下角，固定定位，不支持拖动
- 存在底部导航栏时：悬浮按钮距底部导航栏上边界 **12pt**，距右边界 **12pt**
- 无底部导航栏时：悬浮按钮距屏幕底部 **32pt**
- 多个悬浮按钮纵向排列时，按钮之间间距 **14pt**

### 颜色规范

#### 背景色

| 用途 | 色值 | 透明度 |
|------|------|--------|
| 悬浮按钮背景 | `#FFFFFF` | 90% |

#### 图标颜色（按业务状态）

| 状态 | 色值 | 适用场景 |
|------|------|---------|
| 常规色 | `#767676` | 默认状态，0 件加购时 |
| 黑色 | `#000000` | 已加购商品，强调状态 |
| 绿色（免邮色）| `#198055` | 购物车商品满足免邮条件 |
| 折扣色 | `#FA6338` | 购物车商品存在促销折扣 |

### 场景状态

| 场景 | 图标颜色 | 说明 |
|------|---------|------|
| 0 加车 | `#767676` | 用户未加购任何商品 |
| 1 件商品加车 | `#000000` | 用户已加购商品 |
| 购物车商品免邮 | `#198055` | 已满足免邮门槛 |
| 购物车商品有促销折扣 | `#FA6338` | 购物车有可用促销 |

### 交互规范

#### 展示规则

| 展示规则 | 说明 |
|---------|------|
| 默认展示 | 模块适用于该页面组合时，默认展示 |
| 单按钮展示 | 页面只有一个悬浮按钮时，直接展示 |
| 数量限制 | 超过 3 个时需折叠/合并，避免遮挡页面内容 |

- 当页面不适用独立悬浮按钮时，改为页面内固定按钮
- **同一页面悬浮按钮数量不超过 3 个**

### 禁止事项

- 禁止悬浮按钮支持手动关闭
- 禁止悬浮按钮支持拖动改变位置
- 禁止同一页面超过 3 个悬浮按钮同时展示
- 禁止悬浮按钮文案超过 60 字符

---

## 交易链路场景示意

| 页面 | 按钮类型 | 典型文案 |
|------|---------|---------|
| 列表页 | 商品卡片内 CTA | `Add`、进入商详 |
| 商详页 | 底部全宽主按钮 | `Add to Cart` |
| 购物车 | 底部结算按钮 + 卡片内操作 | `Checkout`、`View More`、`Go to shop` |
| 下单页 | 底部主操作 | `Place Order`、`Continue` |
| 订单详情 | 操作性按钮 | `To Pay`、`View` |
| 活动/礼品卡 | 活动色 CTA | `Grab Now`、`Check Balance`、`Link Card` |

---

## 组件 Schema（AI 调用参考）

### 主按钮（Main Button）

```json
{
  "componentName": "MainButton",
  "figmaComponentKey": "【填入 Figma 组件 Key】",
  "description": "触发全局核心操作的主按钮。通过 variant 区分操作优先级，每屏 Primary 最多出现一个以保证视觉焦点清晰。支持主标题+辅助引导信息组合展示。",
  "properties": {
    "variant": {
      "type": "string",
      "enum": ["primary", "secondary", "ghost", "danger", "text"],
      "description": "primary=唯一主操作；secondary=次要操作；ghost=低优先级并列；danger=不可逆破坏性操作；text=内联操作或链接"
    },
    "size": {
      "type": "string",
      "enum": ["44", "40", "30", "28", "24", "20"],
      "default": "40",
      "description": "按钮高度（pt）：44/40=页面底部主操作（16/14pt 字号）；30=常规；28=紧凑；24/20=极小场景"
    },
    "state": {
      "type": "string",
      "enum": ["default", "active", "disabled", "loading"],
      "default": "default",
      "description": "active=点击态（整体 60% 不透明度）；disabled=禁用态；loading=异步进行中"
    },
    "backgroundStyle": {
      "type": "string",
      "enum": ["dark", "outline", "white", "inverted"],
      "default": "white",
      "description": "按钮所在背景类型：dark=深色背景（白字）；outline=线框样式（白底深字）；white=白底（黑字）；inverted=反白（深灰底）"
    },
    "activityColor": {
      "type": "string",
      "enum": ["none", "#FF3351", "#FF4242"],
      "default": "none",
      "description": "活动主题色覆盖：#FF3351=新人/海外活动；#FF4242=礼品卡场景；none=使用默认品牌色"
    },
    "fullWidth": {
      "type": "boolean",
      "default": false,
      "description": "是否撑满父容器宽度；移动端底部主操作按钮须设为 true，高度配合使用 size=40"
    },
    "label": {
      "type": "string",
      "description": "按钮主标题文案，长文案末尾省略显示"
    },
    "subLabel": {
      "type": "string",
      "description": "辅助引导决策信息（可选），仅展示 1 条；多条时上下轮播展示。用于强化主操作决策"
    },
    "showIcon": {
      "type": "boolean",
      "default": false,
      "description": "是否在主标题前展示单色图标，用于进一步强化主操作引导"
    }
  },
  "slots": {},
  "usageRules": [
    "同一屏幕内 variant=primary 的按钮最多出现 1 个",
    "同一屏幕内 variant=danger 的按钮最多出现 1 个，且必须配合 Modal 二次确认",
    "点击态统一为整体 60% 不透明度，不单独设计每个变体的点击色",
    "表单提交前未满足必填条件时，主操作按钮须设为 state=disabled",
    "移动端底部主操作按钮须设 size=40 且 fullWidth=true",
    "弹窗内按钮须使用固定宽度，不随文案自由拉伸",
    "并列按钮组（如取消+确认）须等宽排列",
    "subLabel 最多同时展示 1 条，多条须轮播，不可堆叠显示",
    "showIcon=true 时图标须为单色，不可使用多色图标"
  ]
}
```

### 局部操控按钮（Section Control Button）

```json
{
  "componentName": "SectionControlButton",
  "figmaComponentKey": "【填入 Figma 组件 Key】",
  "description": "用于页面局部模块/区域的操作控制按钮，以局部操作为主，不承担全局主操作职责。支持文案+图标、纯文字、纯图标三种内容样式，允许自定义导角。",
  "properties": {
    "size": {
      "type": "string",
      "enum": ["36", "30", "28", "24", "20"],
      "default": "30",
      "description": "按钮高度（pt）：36=大号（14pt/20×20pt图标）；30=中号（12pt/20×20pt图标）；28=中小号（12pt/12×12pt图标）；24=小号（10pt/12×12pt图标）；20=极小号（10pt/12×12pt图标）"
    },
    "contentType": {
      "type": "string",
      "enum": ["label-icon", "label", "icon"],
      "default": "label",
      "description": "内容样式：label-icon=文案+图标（用于信息层级复杂场景）；label=纯文字；icon=纯图标"
    },
    "state": {
      "type": "string",
      "enum": ["default", "active", "disabled", "loading"],
      "default": "default",
      "description": "active=点击态（整体 60% 不透明度）；disabled=禁用态；loading=异步进行中"
    },
    "backgroundStyle": {
      "type": "string",
      "enum": ["dark", "outline", "white", "inverted"],
      "default": "white",
      "description": "按钮所在背景类型，与主按钮规范一致"
    },
    "cornerRadius": {
      "type": "number",
      "description": "导角半径（pt），允许自定义，但必须为双数（如 2、4、6、8…）；不填时使用默认圆角"
    },
    "spacing": {
      "type": "number",
      "description": "按钮内容与边缘间距（pt），为最小间距基准，可根据实际场景自定义，但必须为双数"
    },
    "label": {
      "type": "string",
      "description": "按钮文案，contentType=label 或 label-icon 时生效，长文案末尾省略"
    }
  },
  "slots": {
    "icon": {
      "description": "图标插槽，contentType=label-icon 或 icon 时生效",
      "acceptedComponents": ["Icon"],
      "sizeRule": "size>=30 时图标 20×20pt；size<30 时图标 12×12pt"
    }
  },
  "usageRules": [
    "局部操控按钮不承担全局主操作，禁止替代主按钮使用",
    "点击态统一为整体 60% 不透明度，与主按钮一致",
    "cornerRadius 必须为双数，禁止使用奇数圆角值",
    "spacing（间距）必须为双数，可根据场景自定义但不可低于最小间距基准",
    "contentType=label-icon 适用于多子模块或信息层级复杂页面，需通过差异化样式体现层级",
    "文案过长时末尾省略，不换行，不破坏按钮高度"
  ]
}
```

### 悬浮按钮（FAB）

```json
{
  "componentName": "FloatingButton",
  "figmaComponentKey": "【填入 Figma 组件 Key】",
  "description": "固定在页面右下角的悬浮按钮，提供快捷操作入口。根据业务状态切换图标颜色，不支持拖动和手动关闭。",
  "platform": "mobile-only",
  "properties": {
    "shape": {
      "type": "string",
      "enum": ["circle", "vertical"],
      "default": "circle",
      "description": "circle=纯图标圆形（44×44px）；vertical=图标+文案竖版（44×54px）"
    },
    "iconColor": {
      "type": "string",
      "enum": ["#767676", "#000000", "#198055", "#FA6338"],
      "default": "#767676",
      "description": "#767676=常规/0加购；#000000=已加购；#198055=免邮；#FA6338=有促销折扣"
    },
    "label": {
      "type": "string",
      "description": "shape=vertical 时的文案，最多 60 字符，超出省略"
    },
    "bottomOffset": {
      "type": "string",
      "enum": ["with-tabbar", "standalone"],
      "default": "standalone",
      "description": "with-tabbar=距底部导航栏上边界 12pt；standalone=距屏幕底部 32pt"
    }
  },
  "slots": {},
  "usageRules": [
    "固定于屏幕右下角，不支持拖动",
    "不支持手动关闭",
    "同一页面悬浮按钮数量不超过 3 个",
    "超过 3 个时需折叠合并展示",
    "vertical 文案最多 60 字符，超出省略"
  ]
}
```
