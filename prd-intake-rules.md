# 提需阶段准入规则

> 来源：PRD.md 模板提炼 | 更新：2026-07-07  
> 用途：Agent 1（产品助手）接收产品需求时，对输入内容做完整性校验。  
> 所有「必填」字段缺失时，Agent 1 应主动追问，不得跳过进入设计环节。

---

## 一、必填字段（缺一不可）

### 1. 需求背景（PRD § 01）

| 字段 | 要求 | 校验标准 |
|------|------|---------|
| **需求名称** | 格式：`功能/模块 - 简短说明` | 不超过 20 字，含页面/模块定位 |
| **需求背景** | 说明需求来源：用户反馈 / 数据发现 / 业务战略 / 竞品对标，四选一或组合 | 不允许空泛描述如"产品需要做" |
| **需求概述** | 明确：做什么 + 面向谁 + 解决什么问题，三要素齐全 | 缺少任一要素需追问 |
| **需求类型** | 三选一：上游业务提需 / 产品自研 / 用户体验优化 | 影响后续归因和指标选取 |
| **需求 UG** | 提供 UG 单链接 | 无 UG 单不得进入设计流程 |

### 2. 需求范围（PRD § 02）

| 字段 | 要求 | 校验标准 |
|------|------|---------|
| **页面 / 模块** | 明确到具体页面和模块，如「商详-价格区」 | 不允许「全站」等模糊范围 |
| **站点** | 全站 或 具体市场（如 FR / DE） | 涉及合规市场需触发合规知识库加载 |
| **端** | APP / PWA / 两端均有 | 影响组件和布局规范选取 |
| **品类** | 全品类 或 具体品类 | 影响商卡样式等组件差异 |
| **用户** | 全量用户 / 新客 / 会员 等 | 影响个性化逻辑判断 |

### 3. 需求目标（PRD § 03）

| 字段 | 要求 | 校验标准 |
|------|------|---------|
| **业务目标** | 指向可量化的业务指标（如 CVR / GMV / 加购率） | 不允许"提升用户体验"等无法量化的描述 |
| **体验目标** | 说明用户体验底线或解决的体验问题类型 | 需与业务目标互相区分，不可复用同一描述 |

---

## 二、交互介入前必须补充（设计启动前给到）

### 4. 流程图 / 系统交互图（PRD § 04）

> 复杂业务逻辑时必须提供，由产品提出，设计介入前完成。

需覆盖以下三个维度，缺失时 Agent 2（交互助手）需主动提示产品补充：

| 维度 | 内容要求 |
|------|---------|
| **核心流程** | 用户完整操作路径，有明确起点和终点 |
| **边界 & 极值** | 异常场景、空态、错误态、极端数据情况 |
| **信息优先级** | 多信息并存时的展示优先级（如履约信息 vs 商品基础信息） |

---

## 三、不属于提需阶段的内容（不得提前要求）

| 内容 | 所属阶段 |
|------|---------|
| 后端服务设计（§ 05） | 技术评审阶段 |
| 需求详情 / ABT（§ 06） | 需求评审阶段 |
| 多语言文案（§ 07） | 需求评审阶段 |
| 埋点需求（§ 08） | 设计定稿阶段 |
| 计划实验方案（§ 09） | 设计定稿阶段 |
| 数据回收计划（§ 10） | 上线后 |

---

## 四、Agent 1 追问话术模板

当必填字段缺失时，按以下优先级追问（一次不超过 3 个问题）：

```
优先级 1 — 需求概述三要素缺失：
"你的需求描述里还缺少 [做什么/面向谁/解决什么问题]，能补充一下吗？"

优先级 2 — 范围不明确：
"请确认一下影响范围：具体是哪个页面的哪个模块？覆盖哪些站点？"

优先级 3 — 目标不可量化：
"业务目标需要对应具体指标，比如 CVR / 加购率 / GMV，你预期影响哪个？"

优先级 4 — 合规市场触发：
"检测到涉及 [FR/DE/EU] 市场，需要同步确认是否有合规限制，请提供市场合规说明或标注豁免原因。"
```

---

## 五、合规市场自动触发规则

站点字段包含以下市场时，自动加载对应合规知识库：

| 市场 | 触发文件 |
|------|---------|
| 欧盟所有站点（含 FR / DE / NL 等） | `regulations/合规知识库-欧盟通用EU.md` |
| 法国（FR） | `regulations/合规知识库-法国站FR.md` |
